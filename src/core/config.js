window.PAPERS_CONFIG = {
    MAIN_TICK: 5000,
    CLICK_TICK: 10,
    WIRE_RESERVE: 500,
    SAFE_PRICE_FLOOR: 0.05,
    // マーケティング優先度の設定
    MARKETING_MIN_LEVEL: 1, 
    MAX_UNSOLD_THRESHOLD: 5000 // これを超えたらマーケティングを最優先
};